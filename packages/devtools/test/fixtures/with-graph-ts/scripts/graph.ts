// Legacy form: pre-Shape-B projects had `const modules = [...]` here.
// Devtools' fallback expects this file to export `modules`.
export const modules = [
  {
    name: 'users',
    provides: {},
    requires: [],
  },
];
